class CartItem {
  constructor(productId, quantity, image, productPrice, productTitle) {
    this.productId = productId;
    this.quantity = quantity;
    this.image = image;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    // this.sum = sum;
  }
}

export default CartItem;
